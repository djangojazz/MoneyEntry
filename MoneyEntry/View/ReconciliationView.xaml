<UserControl x:Class="MoneyEntry.View.ReconciliationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             Background="Black"
             MaxHeight="850">
    <UserControl.Resources>
        <ResourceDictionary Source="..\MainWindowResources.xaml" />
    </UserControl.Resources>
    <DockPanel>
        <Border DockPanel.Dock="Top">
            <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0">
                        <Label Content="StartDate: " HorizontalAlignment="Center"/>
                        <Border>
                            <DatePicker Height="30" Margin="10" Foreground="{StaticResource FontBrush}" FontSize="20"
                                            SelectedDate="{Binding RefreshStart}" />
                        </Border>
                    </StackPanel>

            <StackPanel Grid.Row="0" Grid.Column="1">
                        <Label Content="EndDate: " HorizontalAlignment="Center"/>
                        <Border >
                            <DatePicker Height="30" Margin="10" Foreground="{StaticResource FontBrush}" FontSize="20"
                                            SelectedDate="{Binding RefreshEnd}" />
                        </Border>
                    </StackPanel>
            </Grid>
        </Border>
        <Button DockPanel.Dock="Top" x:Name="butRefresh" Content="Refresh"  Style="{StaticResource GlassButton}"  Command="{Binding Path=RefreshCommand}" Margin="80,0" />

            <DataGrid DockPanel.Dock="Top" x:Name="datagrid" ItemsSource="{Binding MoneyEnts}" AutoGenerateColumns="False" HorizontalAlignment="Left" VerticalAlignment="Top" 
                              IsSynchronizedWithCurrentItem="True" MaxHeight="700">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Id" Binding="{Binding TransactionId}" CanUserReorder="True"/>
                        <DataGridTextColumn Header="EntryDate" Binding="{Binding CreatedDate, StringFormat={}{0:MM/dd/yyyy}}" CanUserReorder="True"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding TransactionDesc}" Width="250" />
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}"/>
                <!--<DataGridComboBoxColumn Header="Category Combo" DisplayMemberPath="CategoryName" SelectedValuePath="CategoryId"  SelectedValueBinding="{Binding CategoryId}">
                    <DataGridComboBoxColumn.ElementStyle>
                        <Style TargetType="{x:Type ComboBox}">
                            <Setter Property="ItemsSource" Value="{Binding Path=DataContext.Categories, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" />
                        </Style>
                    </DataGridComboBoxColumn.ElementStyle>
                    <DataGridComboBoxColumn.EditingElementStyle>
                        <Style TargetType="{x:Type ComboBox}">
                            <Setter Property="ItemsSource" Value="{Binding Path=DataContext.Categories, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" />
                        </Style>
                    </DataGridComboBoxColumn.EditingElementStyle>
                </DataGridComboBoxColumn>-->
                        <DataGridTextColumn Header="Category" Binding="{Binding Category}" CanUserReorder="True"/>
                        <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:#,0.00}}"  />
                        <DataGridTextColumn Header="RunningTotal" Binding="{Binding RunningTotal, StringFormat={}{0:#,0.00}}" />
                        <DataGridCheckBoxColumn Header="Reconciled" Binding="{Binding Reconciled}" CanUserReorder="True"/>
                    </DataGrid.Columns>
                </DataGrid>
            
        
    </DockPanel>
</UserControl>
